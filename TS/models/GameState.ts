/**
 * Game state enum for quiz state management
 */
export enum GameState {
    INTRO = "INTRO",           // Show introduction
    QUESTION = "QUESTION",     // Show questions
    END = "END"               // Show results
}
